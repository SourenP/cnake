#ifndef DEFS_H_
#define DEFS_H_

#define MIN(a, b) (((a) < (b)) ? (a) : (b))
#define MAX(a, b) (((a) > (b)) ? (a) : (b))

#define GAME_SLEEP 30000

#define GAME_WIDTH 40
#define GAME_HEIGHT 20

#define BORDER_THICKNESS 1

#endif // DEFS_H_